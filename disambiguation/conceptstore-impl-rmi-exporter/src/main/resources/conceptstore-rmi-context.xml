<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
	xmlns:context="http://www.springframework.org/schema/context" xmlns:aop="http://www.springframework.org/schema/aop"

	xsi:schemaLocation="
		http://www.springframework.org/schema/beans	http://www.springframework.org/schema/beans/spring-beans-3.0.xsd
		http://www.springframework.org/schema/context http://www.springframework.org/schema/context/spring-context-3.0.xsd 
		http://www.springframework.org/schema/aop http://www.springframework.org/schema/aop/spring-aop-3.0.xsd"
	default-init-method="init" default-destroy-method="destroy">
	
	<import resource="classpath:conceptstore-impl-context.xml" />
	<context:property-placeholder location="conceptstore-rmi-conf.properties" />
	<bean class="org.springframework.remoting.rmi.RmiServiceExporter">
		<!-- does not necessarily have to be the same name as the bean to be exported -->
		<property name="serviceName" value="indexService" />
		<property name="service" ref="indexService" />
		<property name="serviceInterface" value="org.biosemantics.disambiguation.service.IndexService" />
		<!-- defaults to 1099 -->
		<property name="registryPort" value="${rmi.port}" />
	</bean>
	
	<bean class="org.springframework.remoting.rmi.RmiServiceExporter">
		<!-- does not necessarily have to be the same name as the bean to be exported -->
		<property name="serviceName" value="indexService" />
		<property name="service" ref="indexService" />
		<property name="serviceInterface" value="org.biosemantics.disambiguation.service.IndexService" />
		<!-- defaults to 1099 -->
		<property name="registryPort" value="${rmi.port}" />
	</bean>
	
	<bean class="org.springframework.remoting.rmi.RmiServiceExporter">
		<!-- does not necessarily have to be the same name as the bean to be exported -->
		<property name="serviceName" value="labelStorageService" />
		<property name="service" ref="labelStorageService" />
		<property name="serviceInterface" value="org.biosemantics.conceptstore.common.service.LabelStorageService" />
		<!-- defaults to 1099 -->
		<property name="registryPort" value="${rmi.port}" />
	</bean>
	
	<bean class="org.springframework.remoting.rmi.RmiServiceExporter">
		<!-- does not necessarily have to be the same name as the bean to be exported -->
		<property name="serviceName" value="notationStorageService" />
		<property name="service" ref="notationStorageService" />
		<property name="serviceInterface" value="org.biosemantics.conceptstore.common.service.NotationStorageService" />
		<!-- defaults to 1099 -->
		<property name="registryPort" value="${rmi.port}" />
	</bean>
	
	<bean class="org.springframework.remoting.rmi.RmiServiceExporter">
		<!-- does not necessarily have to be the same name as the bean to be exported -->
		<property name="serviceName" value="noteStorageService" />
		<property name="service" ref="noteStorageService" />
		<property name="serviceInterface" value="org.biosemantics.conceptstore.common.service.NoteStorageService" />
		<!-- defaults to 1099 -->
		<property name="registryPort" value="${rmi.port}" />
	</bean>
	
	<bean class="org.springframework.remoting.rmi.RmiServiceExporter">
		<!-- does not necessarily have to be the same name as the bean to be exported -->
		<property name="serviceName" value="conceptStorageService" />
		<property name="service" ref="conceptStorageService" />
		<property name="serviceInterface" value="org.biosemantics.conceptstore.common.service.ConceptStorageService" />
		<!-- defaults to 1099 -->
		<property name="registryPort" value="${rmi.port}" />
	</bean>
	
	<bean class="org.springframework.remoting.rmi.RmiServiceExporter">
		<!-- does not necessarily have to be the same name as the bean to be exported -->
		<property name="serviceName" value="conceptQueryService" />
		<property name="service" ref="conceptQueryService" />
		<property name="serviceInterface" value="org.biosemantics.conceptstore.common.service.ConceptQueryService" />
		<!-- defaults to 1099 -->
		<property name="registryPort" value="${rmi.port}" />
	</bean>
	
	<bean class="org.springframework.remoting.rmi.RmiServiceExporter">
		<!-- does not necessarily have to be the same name as the bean to be exported -->
		<property name="serviceName" value="relationshipStorageService" />
		<property name="service" ref="relationshipStorageService" />
		<property name="serviceInterface" value="org.biosemantics.conceptstore.common.service.RelationshipStorageService" />
		<!-- defaults to 1099 -->
		<property name="registryPort" value="${rmi.port}" />
	</bean>
</beans>